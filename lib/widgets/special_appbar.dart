
import 'package:flutter/material.dart';

import '../models/items_model.dart';

class SpecialAppBar extends StatelessWidget {
  final Items? model;

  const SpecialAppBar({Key? key, this.model}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
